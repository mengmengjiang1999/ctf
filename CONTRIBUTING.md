# 贡献指南

感谢你帮助完善这份 CTF 学习归档。仓库接受题面勘误、来源补充、可复现解法、目录整理和安全修正。

## 提交内容

每道题尽量保持独立目录，并包含：

- `README.md`：赛事、题名、方向、题面来源、思路、复现步骤与预期结果；
- 原始附件：保留文件名，并由 `docs/SHA256SUMS` 记录 SHA-256；
- `solve.py` 或等价入口：默认离线、本地运行，远程目标通过参数或环境变量显式传入；
- 必要的依赖版本或容器配置。

请区分官方题面、自己的推断和第三方 writeup。引用外部内容时保留链接与作者信息，不复制整篇文章。不要提交未经许可重新分发的材料。

## 安全与隐私

- 允许保存已经结束的公开赛题 flag，但须在 README 中明确提示剧透。
- 不得提交真实账号凭据、API Token、私钥、个人数据或仍在进行中的比赛答案。
- 利用代码仅用于 CTF、靶场和其他明确授权环境；不得把历史远程地址设为默认目标。
- 未知二进制应在无网络、低权限、一次性的隔离环境中分析。

## 提交前检查

从仓库根目录运行：

```bash
python3 scripts/public_audit.py
python3 scripts/generate_challenge_docs.py
git diff --check
```

确认生成器没有产生未提交的清单或哈希变化。新增第三方材料时同步更新 `THIRD_PARTY_NOTICES.md`。
